{
  "frontend/src/App.tsx": "Main application entry point that sets up TanStack Router with all five routes (Today, Dashboard, Leaves, Analytics, Export), wraps everything in AuthGuard for authentication, renders ProfileSetup modal for first-time users, and initializes notification reminders.",
  "frontend/src/pages/Export.tsx": "Export page allowing users to download attendance records as CSV or Excel (TSV) for current week, current month, or all records, with a data preview table and column summary.",
  "frontend/src/index.css": "Global stylesheet with OKLCH color system for light/dark themes, plus new Apple Calendar Overlay CSS classes (.apple-cal-overlay, .apple-cal-card, .apple-cal-selected, .apple-cal-today) implementing the 40% transparent backdrop, solid opaque card surface, and smooth slide-up/fade-in/out transitions.",
  "frontend/src/utils/hoursCalculation.ts": "Pure calculation utility restoring all original exported functions/constants (formatHoursDisplay, formatDateKey, getWeekRange, getMonthRange, isWeekendDate, parseDateKey, calculateWeeklyTarget, checkCoreHoursViolation, getLeaveTypeLabel, getCurrentTimeString, etc.) while adding a LUNCH_DEDUCTION constant (30 min) that is applied in calculateDailyHours for noLeave days only, and factored into calculateSwipeOutPrediction's raw-time calculation.",
  "frontend/src/components/SmartSwipeOutPrediction.tsx": "Restored to its original props interface (swipeIn, breakfastAtOffice, completedMinutesThisWeek, weeklyTargetMinutes) with an added optional leaveType prop that is forwarded to calculateSwipeOutPrediction so the lunch deduction is correctly accounted for in the predicted swipe-out time on regular working days.",
  "frontend/index.html": "Production-ready HTML entry point for SwipeTrack Pro with proper meta tags (description, OG, Apple mobile), favicon referencing the generated swipetrack icon asset, and updated page title.",
  "frontend/src/components/AppleCalendarOverlay.tsx": "New Apple-style full-screen calendar overlay component that renders as a bottom sheet with a 40% transparent blurred backdrop, smooth slide-up/fade animation, month navigation, weekday headers, day grid with today indicator and selected-date filled-circle highlight, swipe-down-to-dismiss gesture, and a \"Today\" shortcut button.",
  "frontend/src/pages/DailyEntry.tsx": "Daily attendance entry page updated to replace the Popover/Calendar date picker with the new AppleCalendarOverlay component; tapping the date button opens the full-screen overlay, and selecting a date closes it and updates the form state while preserving all existing form logic and offline sync behavior."
}